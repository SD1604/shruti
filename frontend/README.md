# Frontend — Phase 5

React chat UI. To be scaffolded with Vite once the backend (Phase 4) has a
working `/chat` endpoint to call.

Planned components:
- ChatWindow.jsx
- VerseCard.jsx (shows Sanskrit + translation + attribution)
- SchoolBadge.jsx (tags which tradition an interpretation is from)
