# MCP Server — Phase 2
#
# This will expose the vector store (built in data/ingestion/) as MCP tools:
#   - search_verses(query, text_filter=None)
#   - get_commentary(verse_id, school_of_thought=None)
#
# Not yet implemented. Phase 1 (Gita ingestion) must be working first.
